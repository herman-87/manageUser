spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
springdoc:
  default-produces-media-type: application/json

server:
  servlet:
    context-path: /api/v1

default:
  activation-code:
    length: 6
    expiration-minutes: 15
    characters: 0123456789
    email-address-sender: hermanessoungou@gmail.com
